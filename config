{
    pages_dir   => app->home->rel_dir('public/content'),
    dump_dir    => app->home->rel_dir('public'),
    name        => 'Mojo::Debut',
    copyright   => '',
    cached      => 0,
    perldoc     => 1,
	hypnotoad   => {
		listen  => ['http://*:3000'], 
		workers => 10,  
		proxy   => 1
	}
}
